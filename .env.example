# ---- Site ----
# Used to build shareable links (QR codes, WhatsApp invites, print QR codes).
# Set to your real domain in production, e.g. https://yourdomain.com
NUXT_PUBLIC_SITE_URL=http://localhost:3000

# ---- Firebase (public - safe to expose to the browser) ----
# From Firebase console > Project settings > General > Your apps > SDK setup and configuration
# Enable Authentication (Email/Password + Google) and Firestore.
# NOTE: Firebase Storage is NOT required - cover photo uploads use Cloudinary
# instead (see below), because Firebase Storage now requires the paid Blaze
# plan even for free-tier usage (effective Feb 2026). If you're already on
# Blaze and would rather use Firebase Storage, see storage.rules.
NUXT_PUBLIC_FIREBASE_API_KEY=
NUXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NUXT_PUBLIC_FIREBASE_PROJECT_ID=
NUXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NUXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NUXT_PUBLIC_FIREBASE_APP_ID=

# reCAPTCHA Enterprise site key for Firebase App Check (public, safe to expose).
NUXT_PUBLIC_RECAPTCHA_ENTERPRISE_SITE_KEY=

# ---- Cloudinary (public - safe to expose to the browser) ----
# Free plan, no credit card required: https://cloudinary.com
# 1. Sign up, then find your Cloud Name on the dashboard home page.
# 2. Settings (gear icon) > Upload > Upload presets > Add upload preset
#    > set Signing Mode to "Unsigned" > Save > copy its name.
# Powers the cover photo uploader on /dashboard/editor.
NUXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
NUXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=

# ---- ToyyibPay (server-only - NEVER prefix these with NUXT_PUBLIC_) ----
# Powers the premium plan checkout on /dashboard/billing.
# Register at toyyibpay.com, then Dashboard > API Integration for the key
# and category code. Sandbox accounts use dev.toyyibpay.com for both the
# account and the base URL; production uses toyyibpay.com.
NUXT_TOYYIBPAY_SECRET_KEY=
NUXT_TOYYIBPAY_CATEGORY_CODE=
# Sandbox: https://dev.toyyibpay.com | Production: https://toyyibpay.com
NUXT_TOYYIBPAY_BASE_URL=https://dev.toyyibpay.com

# ---- Firebase Admin SDK (server-only) ----
# Stringified JSON of a Firebase service account key. Used by the payments
# API routes to verify ID tokens and flip wedding.plan/paymentStatus
# server-side (clients are blocked from writing those fields by
# firestore.rules). Generate it in Firebase console > Project settings >
# Service accounts > Generate new private key, then paste the entire JSON
# file content here on one line (in Vercel, multiline values also work).
NUXT_FIREBASE_SERVICE_ACCOUNT_JSON=
